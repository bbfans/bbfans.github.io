# robots.txt

bbfans.github.io publishes `/robots.txt` with explicit rules for general crawlers and common AI crawlers.

Policy:

- Public content is crawlable.
- Build artifacts under `/_astro/` are not intended for crawling.
- Content Signals are declared as `ai-train=no, search=yes, ai-input=no` for the general crawler group and AI crawler groups.

# WebMCP

bbfans.github.io registers WebMCP tools when `navigator.modelContext.registerTool` is available, with a fallback for `navigator.modelContext.provideContext`.

Tools:

- `open_blog_page`
- `open_projects_page`
- `get_site_discovery`

These tools expose the site's key navigation and discovery resources to compatible browser agents.
